-@activities.each do |activity|
  p
    #{link_to activity.owner.username, activity.owner if activity.owner} #{render_activity activity}
    /=link_to activity.trackable.recipe.title, activity.trackable.recipe